#include <string>
#include <vector>

using namespace std;

// All code above (includes & namespace) is to get rid of errors + follow LeetCode's desired input

class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        
    }
};